# 补充测试报告核对与修复

## 审批过期（确认存在，已修复）

部署前本地工单编号 3 仍为 pending，但 expires_at 为 2026-09-14 02:41:14.770277 UTC。原后端只检查 pending，没有检查截止时间。

修复在持有数据库行锁时再次检查截止时间。超期记录保存为 expired，返回 409，不触发智能体恢复任务。并发决定只允许一个成功。列表、详情和等待审批路径会同步过期状态；后台每分钟清理超期工单。已批准、已拒绝和无截止时间的记录不被清理改写。

前端按 UTC 解析后端无时区时间，定时更新超期状态并禁用决定按钮，后台检查仍是最终限制。

## 审批详情（已有实现，改善入口）

原页面已有点击整行展开的处理，以及 payload JSON、任务参数、描述和待执行代码展示。因此不能仅依据报告脚本对箭头的定位判定功能缺失。

箭头改为明确的详情按钮，支持键盘操作、展开状态和关联区域。交互测试确认可以展开看到 task_plan 内容，再关闭详情。

## 模型定价（已有配置能力）

AI Provider 页面展开供应商的模型列表，每个模型有输入、输出两项单价，单位为美元/百万 Token。后端模型配置已支持 input_price_per_million 和 output_price_per_million。未填写时成本显示为空并提示未计入的 Token，避免虚构账单。需要按实际供应商合同填写，不自动猜测价格。

## 不可变归档桶（部署事项）

本地 bucket_missing 提示准确。此次不自动创建不可撤销保留期限的桶，也不将同机 MinIO 声称为独立第三方证据。配置要求见 audit-integrity-hardening.md；历史损坏记录仍需原始证据恢复。

## 验证

审批相关后端 33 项测试通过，包括超期直连审批、清理保留有效记录、等待过期、并发决定和拒绝恢复任务。前端全量 74 项测试通过；修改后的审批页面 7 项测试复验、检查及 Docker 生产构建通过。

部署前备份：/Users/jc/.codex/backups/cyberguard/pre-approval-expiry-fix-20260918.dump。

本地部署验证通过：编号 3 已为 expired，从 pending 列表移除，approver_id 仍为空；尝试批准返回 409。页面正常响应，历史审计内容异常仍为 51 条，未改写历史证据。
