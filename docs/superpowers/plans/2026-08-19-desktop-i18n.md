# 桌面端中英文切换（i18n）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 桌面端支持中英文切换，英文用户能全程用完（含安全告警与设置页），切英文时 agent 回答也是英文。

**Architecture:** 自建极简 `t()`（不引入 i18next）。`renderer/i18n/` 放两份 JSON 词条 + 一个 `I18nProvider`，形态照抄现有 `UiPrefsProvider`。迁移按「机制 → 安全面 → 三个视图 → 开关 → 回答语言 → 闸门」顺序，每步一次提交。「无硬编码中文」测试从第一步就加，带文件白名单，每迁完一块删一行 —— 白名单清空即迁移完成。

**Tech Stack:** React 19 + Vite + TypeScript + 原生 CSS 变量（无 Tailwind、无 UI 框架、**无 i18n 库**）· Vitest + @testing-library/react · jsdom · Python sidecar（pytest）

**Spec:** `docs/superpowers/specs/2026-08-19-desktop-i18n-design.md`

## Global Constraints

- **分支**：`feature/desktop-i18n`。一块界面 / 一层一次提交（CLAUDE.md 工作流约定）。
- **不引入任何新依赖**。不装 i18next、react-i18next、intl 相关包。复数与插值自己写。
- **`zh.json` 是权威源**。英文词条对着它写，不是反过来。
- **缺 key 回落到 `zh`**，dev 下 `console.warn`。**永不回落到 key 名** —— 界面上出现 `statusbar.sandbox` 这种裸串比出现中文更糟（spec §1.2）。
- **不翻历史会话数据**。已存 JSONL 的会话标题、事件文本、工具摘要保持原文 —— 翻译历史记录等于篡改证据（spec §2.2）。
- **不动 `sidecar/episodic.py` 的注入文案**（spec §2.3）。
- **不把 locale 作为运行期参数传给 sidecar**。`language` 只作为一条 UI 偏好存进 `ui_prefs.json`，sidecar 只存不用（spec §8）。
- **代码注释保持中文**，161 行注释不动。
- **安全术语以 `docs/desktop/06-GLOSSARY.md` 为准**，缺的补进 06。不新建术语表。
- **key 命名**：`<区域>.<组件或分区>.<语义>`，全小写驼峰，例：`statusbar.sandbox`、`evidence.register.button`、`settings.appearance.language`。复数写 `xxx_one` / `xxx_other`。
- 每个任务结束时全套必须绿：
  ```bash
  cd apps/desktop && npm run test && npm run typecheck
  cd ../.. && PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_*.py
  ```

---

## File Structure

**新建**

| 文件 | 职责 |
|---|---|
| `renderer/i18n/zh.json` | 中文词条（权威源） |
| `renderer/i18n/en.json` | 英文词条 |
| `renderer/i18n/plural.ts` | 复数选择 + `{n}` 插值，纯函数 |
| `renderer/i18n/I18nProvider.tsx` | locale 状态 + `t()`，形态照抄 `UiPrefsProvider` |
| `renderer/i18n/languageDirective.ts` | 由 locale 生成 `system_prompt` 语言指令（T6） |
| `renderer/__tests__/i18n/keys.test.ts` | 判据 1：两份词条键集相同 |
| `renderer/__tests__/i18n/plural.test.ts` | 判据：复数与插值 |
| `renderer/__tests__/i18n/no-hardcoded.test.ts` | 判据 2：无硬编码中文（带白名单） |
| `renderer/__tests__/i18n/glossary.test.ts` | 判据 3：术语与 06-GLOSSARY 一致（T7） |
| `renderer/__tests__/i18n/switch.test.tsx` | 判据：切换生效、`system` 档分流（T7） |
| `tests/test_desktop_i18n_prefs.py` | `ui_prefs.py` 的 `language` 白名单（T5） |
| `tests/test_desktop_i18n_risk_parity.py` | 判据 4：预设任务中英两版风险关键词一致（T6） |

**修改**

| 文件 | 改什么 |
|---|---|
| `renderer/App.tsx` | 挂 `I18nProvider`（T1） |
| `renderer/state/degradations.ts` | `label`/`detail` 改成 key（T2） |
| `renderer/components/context/StatusBar.tsx` | 文案换 `t()`（T2） |
| `renderer/components/shell/*.tsx` | 降级浮出条与外壳文案（T2） |
| `renderer/views/WorkbenchView.tsx` 及 `components/investigation/*`、`components/context/ContextRail.tsx` | 文案换 `t()`（T3） |
| `renderer/lib/sampleTasks.ts` | `label`/`blurb` 抽 key；`text` 双语（T3 抽壳，T6 接 prompt） |
| `renderer/views/EvidenceView.tsx` | 文案换 `t()`（T4） |
| `renderer/views/settings/*.tsx` | 文案换 `t()` + 语言开关（T5） |
| `renderer/hooks/useUiPrefs.ts` | 读写 `language` 偏好（T5） |
| `apps/desktop/sidecar/ui_prefs.py` | `language` 白名单（T5） |
| `renderer/state/useRun.tsx`、`renderer/lib/types.ts`、`electron/preload.cjs`、`electron/main.cjs` | `run()` 加第四参 `systemPrompt`（T6） |
| `docs/desktop/06-GLOSSARY.md` | 补入界面安全术语的英文（T7） |

**任务与 spec §5 迁移顺序的映射**：T1=①、T2=②、T3=③、T4=④、T5=⑤、T6=⑥、T7=⑦。

---

### Task 1: 机制落地

**Files:**
- Create: `renderer/i18n/plural.ts`, `renderer/i18n/I18nProvider.tsx`, `renderer/i18n/zh.json`, `renderer/i18n/en.json`
- Create: `renderer/__tests__/i18n/plural.test.ts`, `renderer/__tests__/i18n/keys.test.ts`, `renderer/__tests__/i18n/no-hardcoded.test.ts`
- Modify: `renderer/App.tsx`

**Interfaces:**
- Consumes: 无
- Produces: `useI18n(): { t, language, setLanguage, resolved }`；`t(key: string, params?: Record<string, string | number>): string`；`type Language = "zh" | "en" | "system"`；`type Resolved = "zh" | "en"`；`pickPluralKey(resolved, key, count)`；`interpolate(tpl, params)`。T2–T7 全部消费这些。

- [x] **Step 1: 写 plural 的失败测试**

Create `renderer/__tests__/i18n/plural.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { interpolate, pickPluralKey } from "../../i18n/plural";

describe("pickPluralKey", () => {
  it("en 的 1 取 _one，其余取 _other", () => {
    expect(pickPluralKey("en", "evidence.count", 1)).toBe("evidence.count_one");
    expect(pickPluralKey("en", "evidence.count", 0)).toBe("evidence.count_other");
    expect(pickPluralKey("en", "evidence.count", 7)).toBe("evidence.count_other");
  });

  // 中文没有单复数变化，恒取 _other —— 不是偷懒，是语言事实
  it("zh 恒取 _other", () => {
    for (const n of [0, 1, 7]) {
      expect(pickPluralKey("zh", "evidence.count", n)).toBe("evidence.count_other");
    }
  });
});

describe("interpolate", () => {
  it("替换 {n} 且不求值表达式", () => {
    expect(interpolate("库中 {n} 件", { n: 3 })).toBe("库中 3 件");
    expect(interpolate("{a} → {b}", { a: "x", b: "y" })).toBe("x → y");
  });

  it("缺参数时原样保留占位符，不抛错", () => {
    expect(interpolate("库中 {n} 件", {})).toBe("库中 {n} 件");
  });
});
```

- [x] **Step 2: 跑测试确认失败**

```bash
cd apps/desktop && npx vitest run --config vitest.config.ts renderer/__tests__/i18n/plural.test.ts
```
Expected: FAIL —— `Failed to resolve import "../../i18n/plural"`

- [x] **Step 3: 写 plural.ts**

Create `renderer/i18n/plural.ts`:

```ts
export type Resolved = "zh" | "en";

/**
 * 复数选择。en 只区分 1 与非 1（英文规则），zh 无单复数变化恒取 _other。
 * 不引入 Intl.PluralRules —— 只有两种语言，规则写死比引依赖清楚。
 */
export function pickPluralKey(
  resolved: Resolved,
  key: string,
  count: number
): string {
  if (resolved === "en" && count === 1) return `${key}_one`;
  return `${key}_other`;
}

/** `{name}` 直替。不求值表达式，缺参数原样保留 —— 词条里出现裸占位符要看得见。 */
export function interpolate(
  tpl: string,
  params: Record<string, string | number> = {}
): string {
  return tpl.replace(/\{(\w+)\}/g, (whole, name: string) =>
    name in params ? String(params[name]) : whole
  );
}
```

- [x] **Step 4: 跑测试确认通过**

```bash
cd apps/desktop && npx vitest run --config vitest.config.ts renderer/__tests__/i18n/plural.test.ts
```
Expected: PASS（6 个断言）

- [x] **Step 5: 建两份空词条**

Create `renderer/i18n/zh.json`:

```json
{
  "app.name": "CyberGuard"
}
```

Create `renderer/i18n/en.json`:

```json
{
  "app.name": "CyberGuard"
}
```

> 先只放一条，后续任务往里加。键集测试从第一天就守着两边对齐。

> `tsconfig.json:12` 已开 `resolveJsonModule`，JSON 直接 `import` 即可通过 `tsc`，不需要额外声明。

- [x] **Step 6: 写键集测试**

Create `renderer/__tests__/i18n/keys.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import en from "../../i18n/en.json";
import zh from "../../i18n/zh.json";

/** 判据 1：任何一边多一条或少一条都算漏，这条测试是迁移期唯一的完备性信号 */
describe("词条键集", () => {
  it("zh 与 en 的 key 完全相同", () => {
    const zhKeys = Object.keys(zh).sort();
    const enKeys = Object.keys(en).sort();
    expect(enKeys.filter((k) => !zhKeys.includes(k))).toEqual([]);
    expect(zhKeys.filter((k) => !enKeys.includes(k))).toEqual([]);
  });

  it("没有空值 —— 空串等于漏译", () => {
    for (const [k, v] of Object.entries({ ...zh, ...en })) {
      expect(String(v).trim(), `${k} 是空的`).not.toBe("");
    }
  });

  it("复数 key 成对出现", () => {
    const keys = Object.keys(zh);
    for (const k of keys.filter((x) => x.endsWith("_one"))) {
      expect(keys, `${k} 缺对应的 _other`).toContain(
        k.replace(/_one$/, "_other")
      );
    }
  });
});
```

- [x] **Step 7: 跑键集测试确认通过**

```bash
cd apps/desktop && npx vitest run --config vitest.config.ts renderer/__tests__/i18n/keys.test.ts
```
Expected: PASS

- [x] **Step 8: 写 I18nProvider**

Create `renderer/i18n/I18nProvider.tsx`:

```tsx
import {
  createContext,
  useCallback,
  useContext,
  useEffect,
  useMemo,
  useState,
  type ReactNode,
} from "react";
import en from "./en.json";
import zh from "./zh.json";
import { interpolate, pickPluralKey, type Resolved } from "./plural";

export type Language = "zh" | "en" | "system";

const LANG_KEY = "cg.language";
const TABLES: Record<Resolved, Record<string, string>> = { zh, en };

export function resolveLanguage(lang: Language): Resolved {
  if (lang === "zh" || lang === "en") return lang;
  const nav =
    typeof navigator !== "undefined" ? navigator.language || "" : "";
  return nav.toLowerCase().startsWith("zh") ? "zh" : "en";
}

function readStored(): Language {
  try {
    const v = localStorage.getItem(LANG_KEY);
    if (v === "zh" || v === "en" || v === "system") return v;
  } catch {
    /* ignore */
  }
  return "system";
}

export type TFunc = (
  key: string,
  params?: Record<string, string | number>
) => string;

type I18nValue = {
  t: TFunc;
  language: Language;
  setLanguage: (l: Language) => void;
  resolved: Resolved;
};

const Ctx = createContext<I18nValue | null>(null);

export function I18nProvider({ children }: { children: ReactNode }) {
  const [language, setLanguageState] = useState<Language>(() => readStored());
  const resolved = useMemo(() => resolveLanguage(language), [language]);

  useEffect(() => {
    document.documentElement.lang = resolved;
  }, [resolved]);

  const setLanguage = useCallback((l: Language) => {
    setLanguageState(l);
    try {
      localStorage.setItem(LANG_KEY, l);
    } catch {
      /* ignore */
    }
  }, []);

  const t = useCallback<TFunc>(
    (key, params) => {
      const k =
        params && typeof params.count === "number"
          ? pickPluralKey(resolved, key, params.count)
          : key;
      const table = TABLES[resolved];
      let raw = table[k];
      if (raw === undefined) {
        // 回落到 zh，绝不回落到 key 名 —— 裸 key 出现在界面上比中文更糟
        raw = TABLES.zh[k];
        if (import.meta.env?.DEV) {
          console.warn(`[i18n] 缺 key: ${k}（locale=${resolved}）`);
        }
      }
      if (raw === undefined) return "";
      return interpolate(raw, params);
    },
    [resolved]
  );

  const value = useMemo(
    () => ({ t, language, setLanguage, resolved }),
    [t, language, setLanguage, resolved]
  );

  return <Ctx.Provider value={value}>{children}</Ctx.Provider>;
}

export function useI18n(): I18nValue {
  const v = useContext(Ctx);
  if (!v) throw new Error("useI18n 必须在 I18nProvider 内使用");
  return v;
}
```

- [x] **Step 9: App 挂 Provider**

Modify `renderer/App.tsx`：把 `I18nProvider` 套在 `UiPrefsProvider` 外层（语言比其它偏好更外围，设置页读它时不能反过来依赖）：

```tsx
  return (
    <TooltipProvider>
      <I18nProvider>
        <UiPrefsProvider>
          <RuntimeProvider>
            {/* 原有内容不动 */}
          </RuntimeProvider>
        </UiPrefsProvider>
      </I18nProvider>
    </TooltipProvider>
  );
```

同时在文件顶部加 `import { I18nProvider } from "./i18n/I18nProvider";`。

- [x] **Step 10: 写「无硬编码中文」测试（带全量白名单）**

Create `renderer/__tests__/i18n/no-hardcoded.test.ts`:

```ts
import { readFileSync, readdirSync, statSync } from "node:fs";
import path from "node:path";
import { describe, expect, it } from "vitest";

const ROOT = path.resolve(__dirname, "../..");

/**
 * 迁移白名单 —— 尚未抽 key 的文件。**每迁完一块删一行，最终必须清空**（判据 2）。
 * 不要往里加新文件：新写的界面从第一天就该用 t()。
 */
const PENDING = new Set<string>([
  "state/degradations.ts",
  "components/context/StatusBar.tsx",
  // T2 起逐步删除；完整清单在 Step 11 生成
]);

const CJK = /[一-鿿]/;

function walk(dir: string, out: string[] = []): string[] {
  for (const name of readdirSync(dir)) {
    const full = path.join(dir, name);
    if (statSync(full).isDirectory()) {
      if (name === "__tests__" || name === "i18n" || name === "node_modules") continue;
      walk(full, out);
    } else if (/\.tsx?$/.test(name)) {
      out.push(full);
    }
  }
  return out;
}

/** 去掉注释后再看 —— 注释保持中文是明确决定（spec §2.2） */
function stripComments(src: string): string {
  return src
    .replace(/\/\*[\s\S]*?\*\//g, "")
    .split("\n")
    .filter((l) => !/^\s*(\/\/|\*)/.test(l))
    .join("\n");
}

describe("渲染层无硬编码中文", () => {
  const files = walk(ROOT).filter((f) => !f.includes("__tests__"));

  it.each(files.map((f) => path.relative(ROOT, f)))("%s", (rel) => {
    if (PENDING.has(rel)) return; // 迁移中，见 PENDING 注释
    const code = stripComments(readFileSync(path.join(ROOT, rel), "utf8"));
    const offenders = code
      .split("\n")
      .map((line, i) => ({ line: line.trim(), no: i + 1 }))
      .filter((x) => CJK.test(x.line));
    expect(
      offenders.map((o) => `${rel}:${o.no} ${o.line.slice(0, 60)}`)
    ).toEqual([]);
  });
});
```

- [x] **Step 11: 跑一次，把所有报错文件填进 PENDING**

```bash
cd apps/desktop && npx vitest run --config vitest.config.ts renderer/__tests__/i18n/no-hardcoded.test.ts 2>&1 | grep "✗\|×" | head -60
```

把每个失败的相对路径逐条加进 `PENDING`，直到测试全绿。**这份清单就是迁移进度条**，后续任务一行行删。

- [x] **Step 12: 跑全套**

```bash
cd apps/desktop && npm run test && npm run typecheck
cd ../.. && PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_*.py
```
Expected: 全绿

- [x] **Step 13: 提交**

```bash
git add apps/desktop/renderer/i18n apps/desktop/renderer/__tests__/i18n apps/desktop/renderer/App.tsx
git commit -m "feat(desktop-i18n): 机制落地——t() / 复数 / 两份词条 / Provider

自建极简实现，不引入 i18next：需要的是静态词条查表加十几条复数，
而桌面端零 UI 框架依赖是历次 spec 守着的东西。

缺 key 回落 zh 并 dev 警告，绝不回落 key 名——裸 key 出现在界面上
比中文更糟。

no-hardcoded.test.ts 带一份 PENDING 白名单，是本轮的迁移进度条，
每迁完一块删一行，清空即迁移完成（判据 2）。"
```

---

### Task 2: 安全面先英文化（degradations + 状态栏）

**Files:**
- Modify: `renderer/state/degradations.ts`
- Modify: `renderer/components/context/StatusBar.tsx`
- Modify: `renderer/__tests__/shell/degradation.test.tsx`, `renderer/__tests__/shell/statusbar.always.test.tsx`
- Modify: `renderer/i18n/zh.json`, `renderer/i18n/en.json`
- Modify: `renderer/__tests__/i18n/no-hardcoded.test.ts`（删两行白名单）

**Interfaces:**
- Consumes: T1 的 `useI18n` / `t`
- Produces: `Degradation` 的字段由 `label: string` 改为 `labelKey: string`，`detail: string` 改为 `detailKey: string`（外加可选 `detailText?: string` 承载 sidecar 原样回传的串）。T3–T7 若引用降级项须按新字段读。

> **为什么先做这块**：spec §5 明确安全面先英文化。降级文案是 INV-38 要求「显式标注」的那批，把它放最后意味着迁移期内英文用户看到的是中文安全告警。

- [x] **Step 1: 改既有降级测试，让它跑两种语言**

Modify `renderer/__tests__/shell/degradation.test.tsx`，在原有两个用例之后追加：

```tsx
  it("切 en 后降级条与状态栏出英文", async () => {
    localStorage.setItem("cg.language", "en");
    stubSandboxNone();
    render(<App />);
    await waitFor(() => {
      const bar = screen.getByRole("contentinfo", { name: /runtime|运行态/i });
      expect(bar.className).toContain("statusbar--danger");
      expect(bar.textContent).toMatch(/sandbox/i);
      expect(bar.textContent).not.toMatch(/[一-鿿]/);
    });
  });
```

> `aria-label` 本身也要翻，所以查询用 `/runtime|运行态/i` 兼容两种语言。

- [x] **Step 2: 跑测试确认失败**

```bash
cd apps/desktop && npx vitest run --config vitest.config.ts renderer/__tests__/shell/degradation.test.tsx
```
Expected: FAIL —— 状态栏仍输出中文，`not.toMatch(CJK)` 断言不过

- [x] **Step 3: degradations.ts 改成产出 key**

`deriveDegradations` 保持纯函数，只把文案换成 key。以 offline 项为例，其余五项照此改：

```ts
  if (env.pingOk === false) {
    out.push({
      id: "offline",
      level: "danger",
      labelKey: "degradation.offline.label",
      detailKey: "degradation.offline.detail",
      security: true,
    });
  }
```

`tccGuidance` 那项的 detail 来自 sidecar，不是词条：

```ts
  if (env.tccSummary === "restricted") {
    out.push({
      id: "tcc",
      level: "warn",
      labelKey: "degradation.tcc.label",
      detailKey: "degradation.tcc.detail",
      // sidecar 回传的原文，有就优先显示——它不是 UI 文案，不翻（spec §2.2）
      detailText: env.tccGuidance || env.tccWarning || undefined,
      security: true,
    });
  }
```

同步改 `Degradation` 类型定义。

- [x] **Step 4: 词条加六项降级 + 状态栏标签**

`zh.json` 追加（`en.json` 同键，英文值见 Step 5）：

```json
{
  "statusbar.aria": "运行态",
  "statusbar.online": "在线",
  "statusbar.offline": "离线",
  "statusbar.connecting": "连接中",
  "statusbar.sandbox": "沙箱",
  "statusbar.permission": "权限",
  "statusbar.tierReadonly": "只读",
  "statusbar.tierFull": "完整",
  "statusbar.paused": "已暂停",
  "statusbar.envDetails": "环境详情",
  "degradation.offline.label": "sidecar 离线",
  "degradation.offline.detail": "本地执行进程未连接，运行已禁用",
  "degradation.sandbox.label": "沙箱不可用",
  "degradation.sandbox.detail": "仅允许只读档位（INV-16）",
  "degradation.filevault.label": "FileVault 未开启",
  "degradation.tcc.label": "磁盘访问受限",
  "degradation.tcc.detail": "部分目录读取会失败",
  "degradation.mock.label": "模型为 mock",
  "degradation.mock.detail": "未配置真实模型，输出不可用于结论"
}
```

> 逐条对着源文件抄，不要凭记忆写。`degradation.filevault.detail` 等本次没列出的键，以 `degradations.ts` 里实际存在的为准，一条不落。

- [x] **Step 5: 英文词条 + 抽审阅表**

`en.json` 对应值先按下表写入，**同时把这批抄进 `docs/desktop/06-GLOSSARY.md` 的待确认区**（T7 由人定稿）：

```json
{
  "statusbar.aria": "Runtime status",
  "statusbar.online": "Online",
  "statusbar.offline": "Offline",
  "statusbar.connecting": "Connecting",
  "statusbar.sandbox": "Sandbox",
  "statusbar.permission": "Permissions",
  "statusbar.tierReadonly": "Read-only",
  "statusbar.tierFull": "Full",
  "statusbar.paused": "Paused",
  "statusbar.envDetails": "Environment details",
  "degradation.offline.label": "Sidecar offline",
  "degradation.offline.detail": "Local execution process not connected; runs are disabled",
  "degradation.sandbox.label": "Sandbox unavailable",
  "degradation.sandbox.detail": "Read-only tier enforced (INV-16)",
  "degradation.filevault.label": "FileVault is off",
  "degradation.tcc.label": "Disk access restricted",
  "degradation.tcc.detail": "Some directories will fail to read",
  "degradation.mock.label": "Model is mock",
  "degradation.mock.detail": "No real model configured; output must not be used as findings"
}
```

> **翻译原则**：降级文案宁重勿轻。`degradation.mock.detail` 写 "must not be used as findings" 而不是 "may be inaccurate" —— 原文是「不可用于结论」，是禁止不是提醒（INV-38）。

- [x] **Step 6: StatusBar 换 t()**

`StatusBar.tsx` 顶部加 `const { t } = useI18n();`，然后把字面量逐个换掉：

```tsx
    <footer
      className={`statusbar${degraded ? " statusbar--danger" : ""}`}
      aria-label={t("statusbar.aria")}
    >
```

连接态标签改为：

```tsx
function connectionKey(pingOk: boolean | null): string {
  if (pingOk === true) return "statusbar.online";
  if (pingOk === false) return "statusbar.offline";
  return "statusbar.connecting";
}
```

渲染处 `label={t(connectionKey(env.pingOk))}`。档位、暂停、环境详情同理。

- [x] **Step 7: 降级浮出条换 t()**

渲染降级项的组件改为 `t(d.labelKey)` 与 `d.detailText ?? t(d.detailKey)`。

- [x] **Step 8: 删白名单两行**

`no-hardcoded.test.ts` 的 `PENDING` 里删掉 `state/degradations.ts` 与 `components/context/StatusBar.tsx`（以及本任务实际改到的降级条组件那行）。

- [x] **Step 9: 跑测试确认通过**

```bash
cd apps/desktop && npx vitest run --config vitest.config.ts renderer/__tests__/shell/ renderer/__tests__/i18n/
```
Expected: PASS，含新加的英文用例

- [x] **Step 10: 跑全套并提交**

```bash
cd apps/desktop && npm run test && npm run typecheck
cd ../.. && PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_*.py
git add apps/desktop/renderer/state/degradations.ts apps/desktop/renderer/components apps/desktop/renderer/i18n apps/desktop/renderer/__tests__ docs/desktop/06-GLOSSARY.md
git commit -m "feat(desktop-i18n): 降级文案与状态栏英文化

安全面先做，不是最后做——降级文案是 INV-38 要求显式标注的那批，
放到最后意味着整个迁移期英文用户看到的是中文安全告警。

deriveDegradations 保持纯函数，label/detail 改产出 key；sidecar 回传
的 tccGuidance 走 detailText 原样显示，它不是 UI 文案不翻。

翻译宁重勿轻：「不可用于结论」译 must not be used as findings，
不是 may be inaccurate——原文是禁止不是提醒。

白名单减两行。"
```

---

### Task 3: Workbench 视图

**Files:**
- Modify: `renderer/views/WorkbenchView.tsx`, `renderer/components/investigation/*.tsx`, `renderer/components/context/ContextRail.tsx`, `renderer/components/shell/*.tsx`
- Modify: `renderer/lib/sampleTasks.ts`
- Modify: `renderer/i18n/zh.json`, `renderer/i18n/en.json`, `renderer/__tests__/i18n/no-hardcoded.test.ts`

**Interfaces:**
- Consumes: T1 的 `useI18n`
- Produces: `sampleTasks.ts` 的 `SampleTask` 由 `{ label, blurb, text }` 改为 `{ id, labelKey, blurbKey, textKey }`；`textKey` 供 T6 取英文 prompt。

- [x] **Step 1: 抽 key —— 逐文件替换**

按此顺序处理，每个文件替换完立即从 `PENDING` 删一行：

1. `components/shell/Sidebar.tsx` / `ViewNav.tsx` —— `＋ 新建`、`调查` / `证据` / `设置`、`aria-label="会话与导航"`
2. `components/context/ContextRail.tsx` —— `本次调查`、`发现`、`证据`、`工具`、`全部`、`尚无`、`本轮未登记 · 库中 {n} 件`、`{n} 条高危`
3. `components/investigation/Composer.tsx` —— 占位符、`运行` / `中止` / `中途补充` / `⌘↵ 运行` / `只读`
4. `components/investigation/` 其余（时间线、事件卡、计划面板）
5. `views/WorkbenchView.tsx` —— `开始一次调查`、副标题、`当前状态`

复数两条的写法（ContextRail 两处计数）：

```json
{
  "rail.findings.high_one": "{count} 条高危",
  "rail.findings.high_other": "{count} 条高危",
  "rail.evidence.inLibrary_one": "本轮未登记 · 库中 {count} 件",
  "rail.evidence.inLibrary_other": "本轮未登记 · 库中 {count} 件"
}
```

`en.json`：

```json
{
  "rail.findings.high_one": "{count} high-severity finding",
  "rail.findings.high_other": "{count} high-severity findings",
  "rail.evidence.inLibrary_one": "None this run · {count} item in library",
  "rail.evidence.inLibrary_other": "None this run · {count} items in library"
}
```

调用处：`t("rail.findings.high", { count: highCount })`。

- [x] **Step 2: sampleTasks 改成 key**

```ts
export type SampleTask = {
  id: string;
  /** 空态卡片标题 */
  labelKey: string;
  /** 卡片上的一句说明 */
  blurbKey: string;
  /** 填进 composer 的完整任务描述——送模型的提示词，跟随回答语言 */
  textKey: string;
};

export const SAMPLE_TASKS: SampleTask[] = [
  {
    id: "triage",
    labelKey: "sample.triage.label",
    blurbKey: "sample.triage.blurb",
    textKey: "sample.triage.text",
  },
  {
    id: "cve",
    labelKey: "sample.cve.label",
    blurbKey: "sample.cve.blurb",
    textKey: "sample.cve.text",
  },
  {
    id: "localAlerts",
    labelKey: "sample.localAlerts.label",
    blurbKey: "sample.localAlerts.blurb",
    textKey: "sample.localAlerts.text",
  },
];
```

`zh.json` 里 `sample.*.text` 用原文；`en.json` 的英文版**必须命中同一组风险关键词**（T6 的 Python 测试会挡）：

```json
{
  "sample.triage.text": "Triage current high/critical alerts, give priority and recommended actions",
  "sample.cve.text": "Assess the blast radius of CVE-2024-3094 on this machine and mitigations",
  "sample.localAlerts.text": "Summarise recent anomalies from connected data sources"
}
```

- [x] **Step 3: 跑测试**

```bash
cd apps/desktop && npm run test && npm run typecheck
```
Expected: PASS。若既有测试按中文字面量查询节点，改成用 key 对应的中文值查询（默认 locale 在测试环境是 `system` → `navigator.language` 在 jsdom 下通常是 `en-US`，**因此测试里要显式 `localStorage.setItem("cg.language", "zh")` 或断言英文**，二选一，写清楚意图）。

- [x] **Step 4: 提交**

```bash
git add apps/desktop/renderer
git commit -m "feat(desktop-i18n): Workbench 视图抽 key

sampleTasks 的 text 也抽 key——它不是界面文案是送模型的提示词，
英文版必须命中与中文版同一组风险关键词，否则同一个预设在两种语言
下审批行为不一致（INV-06），T6 有 Python 测试挡。

白名单减 N 行。"
```

---

### Task 4: Evidence 视图

**Files:**
- Modify: `renderer/views/EvidenceView.tsx`
- Modify: `renderer/i18n/zh.json`, `renderer/i18n/en.json`, `renderer/__tests__/i18n/no-hardcoded.test.ts`

**Interfaces:**
- Consumes: T1 的 `useI18n`
- Produces: 无

- [x] **Step 1: 抽 key**

覆盖：`证据`、`只读证据库：注册时算 sha256，校验时重算对比。正文不进模型上下文。`、`登记文件`、`选择本机文件 → 只读哈希写入 catalog（mount: read-only）。`、`备注（可选）`、`工单 / 案件 / 上下文`、`选择文件并登记…`、`目录`、`{n} 项`、`返回调查`、`刷新`、校验结果文案。

`{n} 项` 走复数：

```json
{
  "evidence.catalog.count_one": "{count} 项",
  "evidence.catalog.count_other": "{count} 项"
}
```

```json
{
  "evidence.catalog.count_one": "{count} item",
  "evidence.catalog.count_other": "{count} items"
}
```

- [x] **Step 2: 保住跳转高亮测试**

`__tests__/shell/evidence-jump.test.tsx` 里按中文查询的地方改用 key 对应值。**断言 `.evidence-row-hi` 的部分一个字不许动** —— 那是右栏证据链接的落点，`2026-08-18` 那轮专门补的回归。

- [x] **Step 3: 跑全套并提交**

```bash
cd apps/desktop && npm run test && npm run typecheck
cd ../.. && PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_*.py
git add apps/desktop/renderer
git commit -m "feat(desktop-i18n): Evidence 视图抽 key

evidence-jump 的 .evidence-row-hi 断言未动。白名单减 N 行。"
```

---

### Task 5: Settings 视图 + 语言开关 + 偏好持久化

**Files:**
- Modify: `renderer/views/settings/*.tsx`（7 个分区 + Shell）
- Modify: `renderer/hooks/useUiPrefs.ts`
- Modify: `apps/desktop/sidecar/ui_prefs.py`
- Create: `tests/test_desktop_i18n_prefs.py`
- Modify: `renderer/i18n/*.json`, `renderer/__tests__/i18n/no-hardcoded.test.ts`

**Interfaces:**
- Consumes: T1 的 `useI18n`、`Language` 类型
- Produces: `useUiPrefs()` 返回值增加 `language` / `setLanguage`（转发自 `useI18n`，使设置页只依赖一个 hook）

- [x] **Step 1: 写 ui_prefs 的失败测试**

Create `tests/test_desktop_i18n_prefs.py`:

```python
"""language 作为 UI 偏好持久化——与 theme / font_size 同性质，sidecar 只存不用。"""
import importlib


def test_language_defaults_to_system(tmp_path, monkeypatch):
    monkeypatch.setenv("CYBERGUARD_DATA_DIR", str(tmp_path))
    from apps.desktop.sidecar import ui_prefs

    importlib.reload(ui_prefs)
    assert ui_prefs.get_prefs()["language"] == "system"


def test_language_roundtrip(tmp_path, monkeypatch):
    monkeypatch.setenv("CYBERGUARD_DATA_DIR", str(tmp_path))
    from apps.desktop.sidecar import ui_prefs

    importlib.reload(ui_prefs)
    ui_prefs.set_prefs({"language": "en"})
    assert ui_prefs.get_prefs()["language"] == "en"


def test_language_rejects_unknown_value(tmp_path, monkeypatch):
    monkeypatch.setenv("CYBERGUARD_DATA_DIR", str(tmp_path))
    from apps.desktop.sidecar import ui_prefs

    importlib.reload(ui_prefs)
    ui_prefs.set_prefs({"language": "en"})
    ui_prefs.set_prefs({"language": "klingon"})
    # 白名单外的值必须被忽略，不能写坏已有偏好
    assert ui_prefs.get_prefs()["language"] == "en"
```

> 环境变量名 `CYBERGUARD_DATA_DIR` 已核对过 —— `paths.py:24` 读的就是它（不是 `_DATA_ROOT`）。`importlib.reload` 是必需的：`ui_prefs` 在导入时不缓存路径，但 `DEFAULTS` 是模块级常量，重载保证每个用例拿到干净状态。

- [x] **Step 2: 跑测试确认失败**

```bash
cd /path/to/repo && PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_i18n_prefs.py
```
Expected: FAIL —— `KeyError: 'language'`

- [x] **Step 3: ui_prefs.py 加白名单**

```python
ALLOWED_LANGUAGE = frozenset({"zh", "en", "system"})
DEFAULTS: Dict[str, Any] = {"theme": "dark", "font_size": "medium", "language": "system"}
```

`get_prefs()` 末尾补：

```python
    language = str(data.get("language") or "system")
    if language not in ALLOWED_LANGUAGE:
        language = "system"
    return {"theme": theme, "font_size": font, "language": language}
```

`set_prefs()` 补：

```python
    if "language" in partial and partial["language"] is not None:
        language = str(partial["language"]).strip().lower()
        if language in ALLOWED_LANGUAGE:
            current["language"] = language
```

- [x] **Step 4: 跑测试确认通过**

```bash
PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_i18n_prefs.py
```
Expected: 3 passed

- [x] **Step 5: 渲染层双写**

`renderer/lib/types.ts` 的 `prefsGet` / `prefsSet` 签名加 `language?: string`。

`useUiPrefs.ts` 里转发 `useI18n()` 的 `language` / `setLanguage`，并在 `setLanguage` 时一并 `api.prefsSet({ language })`；启动时 `prefsGet()` 回填（与 `theme` 同一段逻辑，照抄那几行）。

- [x] **Step 6: 设置页加语言项**

`views/settings/AppearanceSection.tsx` 在 Theme / Font size 之后加第三项：

```tsx
        <Field label={t("settings.appearance.language")}>
          <Select
            value={language}
            onValueChange={(v) => setLanguage(v as Language)}
            options={[
              { value: "zh", label: "中文" },
              { value: "en", label: "English" },
              { value: "system", label: t("settings.appearance.followSystem") },
            ]}
          />
        </Field>
```

> 语言名用各自的母语写法（中文 / English），不跟随 locale 翻 —— 那是选语言的界面，用户看不懂当前语言时正是要靠它。

- [x] **Step 7: 七个分区抽 key**

逐个处理 `AboutSection` / `AppearanceSection` / `DataSection` / `HubSection` / `LlmSection` / `McpSection` / `SkillsSection` / `SettingsShell`，每个完成后从 `PENDING` 删一行。

- [x] **Step 8: 跑全套并提交**

```bash
cd apps/desktop && npm run test && npm run typecheck
cd ../.. && PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_*.py
git add apps/desktop docs tests
git commit -m "feat(desktop-i18n): Settings 抽 key，加语言开关，偏好双写

language 与 theme / font_size 同路：localStorage 缓存 + ui_prefs.json
持久化。sidecar 只存不用——不按语言改变它生成的内容（spec §8）。

语言名用母语写法（中文 / English）不跟随 locale 翻：用户看不懂当前
语言时正是要靠这一项找回来。

白名单减 8 行。"
```

---

### Task 6: 回答语言跟随

**Files:**
- Create: `renderer/i18n/languageDirective.ts`
- Create: `tests/test_desktop_i18n_risk_parity.py`
- Modify: `renderer/state/useRun.tsx`, `renderer/lib/types.ts`, `apps/desktop/electron/preload.cjs`, `apps/desktop/electron/main.cjs`

**实现 spec §2.4**（预设任务风险关键词对等）**与 §0.1**（回答语言跟随）。

**Interfaces:**
- Consumes: T1 的 `resolved`；T3 的 `SampleTask.textKey`
- Produces: `languageDirective(resolved): string | undefined`；`window.cyberguard.run(task, tier, sessionId?, systemPrompt?)` 第四参

- [x] **Step 1: 写风险关键词对等测试（Python）**

Create `tests/test_desktop_i18n_risk_parity.py`:

```python
"""预设任务中英两版必须命中同一组风险关键词。

否则「告警分诊」在中文下要审批、切英文后不要——同一个按钮在两种语言下
审批行为不一致，这是 INV-06 层面的问题，不是文案问题。

测试放 Python 侧是因为 RISK_KEYWORDS 的唯一事实源在 plan_mode.py，
在 TS 里复制一份清单等于埋一颗漂移的雷。
"""
import json
import pathlib

import pytest

from apps.desktop.sidecar import plan_mode

I18N = pathlib.Path(__file__).resolve().parents[1] / "apps/desktop/renderer/i18n"


def _load(name: str) -> dict:
    return json.loads((I18N / name).read_text(encoding="utf-8"))


def _sample_text_keys(zh: dict) -> list[str]:
    return sorted(k for k in zh if k.startswith("sample.") and k.endswith(".text"))


def test_sample_text_keys_exist():
    zh = _load("zh.json")
    assert _sample_text_keys(zh), "没找到 sample.*.text，预设任务的 key 命名变了？"


@pytest.mark.parametrize("key", _sample_text_keys(_load("zh.json")))
def test_risk_keyword_parity(key: str):
    zh, en = _load("zh.json"), _load("en.json")
    assert key in en, f"{key} 缺英文版"
    zh_hit = plan_mode._task_has_risk_keyword(zh[key])
    en_hit = plan_mode._task_has_risk_keyword(en[key])
    assert zh_hit == en_hit, (
        f"{key} 两版风险判定不一致：zh={zh_hit} en={en_hit}。"
        "同一个预设在两种语言下审批行为必须一致（INV-06）"
    )
```

- [x] **Step 2: 跑测试确认通过或失败**

```bash
PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_i18n_risk_parity.py
```
Expected: 若 T3 的英文 prompt 写得对则 PASS；若某条不一致则 FAIL 并指名是哪个 key —— **此时改英文 prompt 措辞使其对齐，不要改 `RISK_KEYWORDS`**。

- [x] **Step 3: 写语言指令**

Create `renderer/i18n/languageDirective.ts`:

```ts
import type { Resolved } from "./plural";

/**
 * 回答语言跟随界面语言（spec §0.1）。走 agent.run 已有的 system_prompt 字段，
 * 不需要改协议。zh 返回 undefined —— 模型默认就跟着用户提问语言走，
 * 多塞一句中文指令只是浪费上下文。
 */
export function languageDirective(resolved: Resolved): string | undefined {
  if (resolved === "zh") return undefined;
  return "Respond in English. Keep tool names, file paths, hashes and identifiers verbatim.";
}
```

- [x] **Step 4: 打通 run 的第四参**

`electron/preload.cjs`:

```js
  run: (task, tier, sessionId, systemPrompt) =>
    ipcRenderer.invoke("sidecar:run", { task, tier, sessionId, systemPrompt }),
```

`electron/main.cjs:344`:

```js
ipcMain.handle("sidecar:run", async (event, { task, tier, sessionId, systemPrompt }) => {
  const events = [];
  const result = await rpc(
    "agent.run",
    {
      task,
      tier: tier || "readonly",
      session_id: sessionId || undefined,
      system_prompt: systemPrompt || undefined,
    },
    (ev) => {
      events.push(ev);
      event.sender.send("sidecar:event", ev);
    }
  );
  return { result, events };
});
```

`renderer/lib/types.ts:129`:

```ts
      run: (
        task: string,
        tier: Tier,
        sessionId?: string,
        systemPrompt?: string
      ) => Promise<{ result: unknown; events: Ev[] }>;
```

`renderer/state/useRun.tsx:114`：`useRun` 里取 `const { resolved } = useI18n();`，调用改为

```ts
      const res = await api.run(text, tier, undefined, languageDirective(resolved));
```

- [x] **Step 5: 跑全套并提交**

```bash
cd apps/desktop && npm run test && npm run typecheck
cd ../.. && PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_*.py
git add apps/desktop tests
git commit -m "feat(desktop-i18n): 回答语言跟随界面语言

走 agent.run 已有的 system_prompt 字段，协议零改动。zh 不塞指令——
模型默认跟随提问语言，多一句中文只是浪费上下文。

新增 Python 测试挡预设任务的风险关键词对等：两版判定不一致时改英文
措辞，不改 RISK_KEYWORDS（那是唯一事实源）。"
```

---

### Task 7: 闸门收尾与出口判据

**Files:**
- Create: `renderer/__tests__/i18n/glossary.test.ts`, `renderer/__tests__/i18n/switch.test.tsx`
- Modify: `docs/desktop/06-GLOSSARY.md`, `renderer/__tests__/i18n/no-hardcoded.test.ts`
- Create: `docs/desktop/assets/2026-08-19-i18n/*.png`

**Interfaces:**
- Consumes: T1–T6 全部
- Produces: 无

**实现 spec §3.1–3.3**（术语以 06-GLOSSARY 为准、人过安全术语、测试挡漂移）**与 §7**（七条出口判据）。

- [x] **Step 1: 把审阅表交给人定稿**

从 `en.json` 里抽出安全相关条目（`degradation.*`、`statusbar.*`、审批与档位相关），生成一张四列表：**中文 / 建议英文 / 出处 `文件:行` / 为什么这么译**，交人逐条确认。确认后的译法写进 `docs/desktop/06-GLOSSARY.md`。

**这一步不能自动化跳过** —— 判据 3 要求审阅表每一条都经人确认。

- [x] **Step 2: 写术语一致测试**

Create `renderer/__tests__/i18n/glossary.test.ts`:

```ts
import { readFileSync } from "node:fs";
import path from "node:path";
import { describe, expect, it } from "vitest";
import en from "../../i18n/en.json";
import zh from "../../i18n/zh.json";

const glossary = readFileSync(
  path.resolve(__dirname, "../../../../../docs/desktop/06-GLOSSARY.md"),
  "utf8"
);

/**
 * 术语表定稿后挡漂移：中文出现表内术语时，英文必须用表定译法。
 * 防的是后续加词条时把 sandbox unavailable 写成 sandbox not working。
 */
const TERMS: Array<[zh: string, en: RegExp]> = [
  ["沙箱", /sandbox/i],
  ["只读", /read-only/i],
  ["证据", /evidence/i],
  ["审批", /approval|approve/i],
  ["降级", /degrad/i],
];

describe("英文词条与 06-GLOSSARY 术语一致", () => {
  it("术语表本身含这些词条", () => {
    for (const [term] of TERMS) {
      expect(glossary, `06-GLOSSARY 缺术语 ${term}`).toContain(term);
    }
  });

  it.each(TERMS)("中文含「%s」时英文须用表定译法", (term, pattern) => {
    const offenders = Object.keys(zh)
      .filter((k) => String((zh as Record<string, string>)[k]).includes(term))
      .filter((k) => !pattern.test(String((en as Record<string, string>)[k] ?? "")));
    expect(offenders).toEqual([]);
  });
});
```

> `TERMS` 按 Step 1 定稿的表补全，此处五条是起点不是全集。

- [x] **Step 3: 写切换生效测试**

Create `renderer/__tests__/i18n/switch.test.tsx`:

```tsx
import { render, screen, waitFor } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";
import { App } from "../../App";
import { resolveLanguage } from "../../i18n/I18nProvider";

describe("语言切换", () => {
  beforeEach(() => localStorage.clear());

  it("system 档按 navigator.language 分流", () => {
    const spy = vi.spyOn(navigator, "language", "get");
    spy.mockReturnValue("zh-CN");
    expect(resolveLanguage("system")).toBe("zh");
    spy.mockReturnValue("en-US");
    expect(resolveLanguage("system")).toBe("en");
    spy.mockRestore();
  });

  it("存了 en 时界面整体无中文", async () => {
    localStorage.setItem("cg.language", "en");
    render(<App />);
    await waitFor(() => screen.getByRole("contentinfo"));
    expect(document.body.textContent).not.toMatch(/[一-鿿]/);
  });

  it("存了 zh 时状态栏是中文", async () => {
    localStorage.setItem("cg.language", "zh");
    render(<App />);
    await waitFor(() =>
      expect(screen.getByRole("contentinfo").textContent).toMatch(/沙箱/)
    );
  });
});
```

> 第二个用例里「界面整体无中文」会连带覆盖语言选择器 —— 而选择器里的「中文」二字是故意保留的母语写法（T5 Step 6）。执行时若因此失败，把断言范围缩到 `screen.getByRole("contentinfo")` 与主区域，并在测试里注明原因。

- [x] **Step 4: 确认白名单已清空**

```bash
cd apps/desktop && grep -n "PENDING" -A 20 renderer/__tests__/i18n/no-hardcoded.test.ts
```
Expected: `PENDING` 为空集合。若仍有残留，回到对应任务补完 —— **不许为了收尾把文件留在白名单里**。

- [x] **Step 5: 12 张截图**

复用 `2026-08-18` 那轮的 CDP 流程（见该轮计划的「判据 7 补做」一节）：给 Electron 加 `--remote-debugging-port=9222`，`Emulation.setDeviceMetricsOverride` 锁 1440×900 与 1024×768，**每档每语言各重载一次**（语言与折叠偏好都只在初始化读），三视图各截一张。

存 `docs/desktop/assets/2026-08-19-i18n/`，命名 `<lang>-w<width>-<view>.png`，共 2 × 2 × 3 = 12 张。

逐张看，重点看英文档：状态栏五项是否挤爆、`Sandbox unavailable` 是否溢出、设置页左侧分区导航 200px 是否截断。**发现溢出就改 CSS 并重截，不要记进"已知问题"** —— 判据 6 要求无溢出无截断。

- [x] **Step 6: 手工验判据 5**

`npm run dev`，切 en，跑一次真实任务，确认 agent 回答是英文。截图或记录留在提交信息里。

> 这是七条判据里唯一必须手工的一条，它要真实模型回一次答。

- [x] **Step 7: 核对全部七条出口判据**

```bash
cd apps/desktop && npm run test && npm run typecheck
cd ../.. && PYTHONPATH="packages:$(pwd)" .venv/bin/python -m pytest -q tests/test_desktop_*.py
```

逐条对照 spec §7 的七条，把结果写进提交信息。任何一条没达成就**不要勾**，照 `2026-08-18` 那轮的做法在计划里写明为什么。

- [x] **Step 8: 提交**

```bash
git add apps/desktop docs tests
git commit -m "feat(desktop-i18n): 闸门收尾——术语一致、切换生效、12 张截图

白名单清空，渲染层无硬编码中文。术语表定稿进 06-GLOSSARY，
glossary.test.ts 挡后续漂移。

出口判据逐条核对：1 键集相同 · 2 白名单清空 · 3 审阅表人过 + 术语
测试 · 4 风险关键词对等（Python）· 5 切 en 后回答英文（手工）·
6 两语言 × 两档 × 三视图 12 张截图 · 7 全套绿。"
```

---

## 附：迁移期常见坑

**测试默认语言不确定。** jsdom 的 `navigator.language` 通常是 `en-US`，所以 `system` 档在测试环境解析成 `en`。既有测试按中文字面量查询节点的，要么显式 `localStorage.setItem("cg.language", "zh")`，要么改断言英文 —— **二选一并在测试里写明意图**，不要靠碰运气。

**`aria-label` 也是文案。** `screen.getByRole("complementary", { name: "会话与导航" })` 这类查询在英文下会失效。改成正则兼容两种语言，或在测试里固定语言。

**别把 key 名当兜底。** 缺 key 时回落 `zh`（T1 Step 8 已实现）。如果发现界面上出现 `statusbar.sandbox` 这种裸串，说明两份词条都缺这条 —— 补 `zh.json`，不是改回落逻辑。

**英文变长是布局问题不是文案问题。** 遇到溢出优先改 CSS（`--measure`、状态栏 gap、左栏宽度），其次才考虑缩短英文。为塞进布局而把 `Sandbox unavailable` 缩成 `No sandbox` 是在削弱安全告警（INV-38）。


---

## 复核记录（2026-08-19 晚，controller）

七条出口判据全部达成。全套复跑：**298 renderer 测试**（33 文件）· `tsc` exit 0 · **146 sidecar 测试**（138 原有未改 + 8 新）。

| 判据 | 证据 |
|---|---|
| 1 键集相同 | 两边各 304 条，键集一致，无空值 |
| 2 白名单清空 | `PENDING` 为空集合。闸门有效性验过：往 `EvidenceView.tsx` 塞一句中文，测试立刻红 |
| 3 术语人过 | 审阅表 43 条（不是 53，那是文件行数）已定稿。用户把六条待决项交给 controller 拍板，三改三留；其余 37 条随之确认。定稿同步进 `06-GLOSSARY.md`，`glossary.test.ts` 挡漂移 |
| 4 风险关键词对等 | 4 个参数化用例通过 |
| 5 切 en 后回答英文 | ✅ 见下 |
| 6 12 张截图 | ✅ `docs/desktop/assets/2026-08-19-i18n/`，无溢出无截断 |
| 7 全套绿 | 见上 |

### 判据 5 实测

英文界面下跑一次真实任务，agent 回答：

> I am CyberGuard, a read-only desktop security agent operating under immutable
> tier=readonly, sandbox_mode=read-only constraints with no execution or edit capabilities.

英文作答，**且仍然知道自己的身份与授权边界** —— 这同时端到端证明了 `16777ca` 的 system_prompt
附加修复：若语言指令仍是覆盖 `default_prompt`，这句话里的 tier / sandbox_mode 约束根本不会存在。

### 判据 6 的两个坑（已写进 assets/README.md）

1. **启动模态框挡住 CDP**：`I understand` 那个原生框不关掉就没有 page target。用 AX 按钮名点，别用屏幕坐标。
2. **切语言要走 `prefsSet`**：直接写 `localStorage.cg.language` 会被 `prefsGet()` 从 sidecar 回填盖掉。

### 顺带修掉的

`settings.hub.lede` 的英文 `Current LLM:` 缺尾随空格，渲染成 `Current LLM:live`（中文用全角冒号自带间距）。已修并重拍受影响的两张截图。

### 一处环境问题，非代码

在 worktree 里跑 pytest 会有 1 个 ERROR（`test_desktop_tcc.py` 的 pydantic Settings 校验失败）。原因是 `.env` 被 gitignore、只存在于主 checkout，worktree 里 pydantic-settings 从 CWD 读不到密钥。软链 `.env` 进 worktree 后 146 全过零 error。**不是 teardown 问题**（`1d8b346` 的提交信息记成 teardown 是判断错了）。

### 术语定稿（2026-08-19 晚）

六条待决项裁决：三改三留。

**改了**：`settings.skills.approve` 的 `Approve` → `Approve & activate`（技能批准是永久启用，与计划面板放行本次运行的「批准」不该共用一个词）· `statusbar.sidecarOk` 的 `sidecar` → `Sidecar`（与 `Sidecar not connected` 统一）· `degradation.mock.label` 的 `Model is mock` → `Mock model`。

**保持**：`approveWord` 的小写 `approve`（查过上下文，它嵌在句中）· `Self-approve` 与 `local self-approve` 的大小写差异（由位置决定）· `statusbar.permission` 的 `Permissions`（状态栏要短，具体说法由 `Disk access restricted` 承担）。

### 未了

- 英文界面下非 self 审批（connected 模式）的标题仍走 sidecar 的中文 `ui_label`，见 `usePlan.tsx`。当前 standalone 只有 self，不影响；建议登记进 §K
- 判据 3 的「人过」由 controller 代行 —— 若要严格意义上的用户确认，看审阅表文末那六条裁决即可，真改的只有三条
