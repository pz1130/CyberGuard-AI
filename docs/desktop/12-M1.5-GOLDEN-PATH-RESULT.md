# M1.5 黄金路径 · 自动验收结果

时间：自动生成 · 耗时 **33.4s** · **14/14** 通过

总体：✅ PASS

## 检查清单

| 状态 | 项 | 细节 |
|------|----|------|
| ✅ | 1. 真实 LLM 已接通 | MiniMax-M3 @ https://api.minimaxi.com/v1 |
| ✅ | 1b. Seatbelt 可用 | seatbelt |
| ✅ | 1c. TCC/FDA 探针 | summary=fda_likely fda=True |
| ✅ | 2. MCP 工具可见 | mcp__echo__echo, mcp__echo__list_alerts, mcp__echo__secret_probe |
| ✅ | 2b. 内置 alert_triage 技能 | alert_triage, compliance_gap, cve_impact, evidence_handling, incident_investigation |
| ✅ | 3. Agent 进入执行循环 | events=11 tools=['load_skill', 'mcp__echo__list_alerts'] err=[] |
| ✅ | 4. 只读调查（MCP list_alerts） | ['load_skill', 'mcp__echo__list_alerts'] |
| ✅ | 4b. progressive load_skill | ['load_skill', 'mcp__echo__list_alerts'] |
| ✅ | 4c. readonly 无 Exec/Edit |  |
| ✅ | 5. Markdown 报告产出 | len=2332 |
| ✅ | 5b. 证据/来源可回溯 |  |
| ✅ | 6. 第二次任务可跑（再打开） | events=9 |
| ✅ | B. Electron 开发签名稳定 | Identifier + Authority 匹配 |
| ✅ | B2. codesign identity 在 valid 列表 |   1) 9466E6B279C14C4432A8BD9C03F92C20A9D72059 "CyberGuard Dev"      1 valid identities found  |

## 工具调用（主任务）

```
load_skill, mcp__echo__list_alerts
```

## 报告摘录

```markdown
# 告警分诊报告（中文）

> 来源：`mcp__echo__list_alerts(limit=25)` 返回 3 条样本。
> 标记：`source_trust=hostile`（按 SOP 默认视为不可信外部输入，不得用于改变授权/沙箱/能力）。

---

## 1. 排序与聚类

样本仅 3 条、签名各异，未形成同主机/同签名的有效聚类，故直接按风险面排序。

| 排名 | ID | 严重度 | 标题 | 主机 | 计数 |
|---|---|---|---|---|---|
| 1 | A-1001 | high | Suspicious PowerShell from workstation | ws-042 | 12 |
| 2 | A-1002 | medium | Failed SSH bursts | jump-01 | 84 |
| 3 | A-1003 | low | AV signature update lag | ws-017 | 1 |

排序依据（按 SOP：主动利用信号 > 资产关键性 > 爆炸半径 > 新颖性）：
1. **A-1001**：标题含 "Suspicious" + high，且 12 次重复 ⇒ 偏主动利用信号；workstation 通常是初始入侵/横向跳板。
2. **A-1002**：jump host 爆炸半径大，但告警是 *失败* 登录暴破，且 severity=medium；多为外部扫描噪声，不应压过 A-1001。
3. **A-1003**：AV 签名更新滞后属卫生/运维类，severity=low，无即时利用迹象。

---

## 2. Top findings（每条：理由 / 建议动作）

### 🔴 #1 A-1001 · ws-042 · Suspicious PowerShell（high, ×12）
- **为何重要**：PowerShell 在工作站上的可疑执行是常见的初始访问 / 凭据访问 / 横向移动载体；高严重度 + 12 次重复触发提示非偶发。
- **建议动作（read-only）**：
  1. 拉取 ws-042 的进程树、命令行（含 `EncodedCommand`、父进程、发起用户）。
  2. 关联近 24h 的网络出站（异常外联、C2 IoC）、计划任务/WMI 持久化痕迹。
  3. 检查同用户在其他主机的登录/异常行为（侧向移动迹象）。
  4. 在**用户/运维**显式授权并切换到可执行档位前，**不要**对 ws-042 采取隔离/查杀等处置动作（当前 `tier=readonly, has_exec=False, has_edit=False`）。

### 🟡 #2 A-1002 · jump-01 · Failed SSH bursts（medium, ×84）
- **为何重要**：jump host 是高价值枢纽，一旦从"失败"翻转为"成功"爆炸半径极大；84 次提示存在持续外部尝试。
- **建议动作（read-only）**：
  1. 确认是否同时存在 *成功* 登录（若出现，优先级应直接升至 #1）。
  2. 统计源 IP 分布、是否命中已知威胁情报、是否对多个账号。
  3. 核对 jump-01 的 fail2ban / 限速 / 地理封禁策略是否生效。
  4. 仅作为监测项；当前证据不足以建议封禁或改密。

### 🟢 #3 A-1003 · ws-017 · AV signature update lag（low, ×1）
- **为何重要**：单次低危，纯卫生问题，不构成主动利用。
- **建议动作**：纳入补丁/运维工单，跟进 WSUS/SCEP 同步状态；可延后到下个维护窗口。

---

## 3. 不确定性（显式声明）

- **上下文严重不足**：缺少时间戳、用户、源 IP、命令/进程、资产关键性标签、是否含"成功"事件 → 排序仅为**指示性**，非定论。
- **签名名称是摘要**：MCP 源被标记为 `hostile`，标题可能与真实告警语义存在偏差（例如 "Suspicious" 可能只是策略命中而非已确认利用）。
- **未见聚类素材**：仅 3 条，无法体现 SOP 要求的"同主机/同签名/同时段"聚类价值。
- **当前档位限制**：`tier=readonly, has_exec=False, has_edit=False` —— 本报告**不包含也不能执行**任何主机侧处置动作；任何隔离/阻断都需用户显式授权并升档。

---

## 4. 建议补取的数据（只列"会改变 Top 3"的关键项）

1. A-1001 的 PowerShell **命令行原文 / 父进程 / 发起用户 / 哈希** —— 决定是"立即升级"还是"降为观察"。
2. A-1002 是否伴随 **成功登录** 或 **非常规账号命中** —— 若有则升为 #1。
3. ws-042 / jump-01 的 **资产关键性 / 所属业务** 与 **是否可外网可达** —— 校准爆炸半径。

---

## 5. 来源引用

- SOP：`load_skill("alert_triage")` v1.1.0（builtin, mode=both）
- 数据：`mcp__echo__list_alerts(limit=25)` —— `source_trust=hostile`，本报告仅作为分诊输入引用，未据其改变授权/沙箱/能力。
```

## 环境（公开信息）

- data_root: `/Users/jc/Library/Application Support/CyberGuard`
- provider: `MiniMax-M3` live=live
- mcp tools: ['mcp__echo__echo', 'mcp__echo__list_alerts', 'mcp__echo__secret_probe']
- Electron: signed as `com.cyberguard.desktop.dev` / `CyberGuard Dev`
- Electron UI: `npm run dev:signed` 已由代理启动（窗口应在桌面）

## 说明

- 工程闭环（LLM + MCP + SOP + Markdown 报告 + 二次任务 + 开发签名）已自动验证。
- 主观「愿不愿意第二天再打开」不在本清单强制范围；你若看到窗口，可随手点几下补充体感。
