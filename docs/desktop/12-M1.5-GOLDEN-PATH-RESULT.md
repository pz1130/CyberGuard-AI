# M1.5 黄金路径 · 验收结果

## 最新（合并 UI 后 · headless 复验）

时间：2026-08-04 · 耗时 **35.0s** · **6/6 检查通过** · 总体：✅ PASS  
分支：`main` @ merge PR #16（Desktop UI P0–P3）

| 状态 | 项 | 细节 |
|------|----|------|
| ✅ | Provider live | MiniMax-M3 @ minimaxi.com/v1 · `has_api_key`（已 migrate 出 provider.json 明文） |
| ✅ | Demo MCP 一键安装 | `mcp.config.install_demo` → id=`echo` |
| ✅ | MCP 工具可见 | `mcp__echo__echo`, `mcp__echo__list_alerts`, `mcp__echo__secret_probe` |
| ✅ | Evidence 注册/校验 | register + verify OK |
| ✅ | 分诊主任务 | tools=`load_skill`, `mcp__echo__list_alerts` · answer ~1888 字 Markdown |
| ✅ | 第二次任务（再打开） | `answer_ready` · 11 events |
| ✅ | 敌对来源意识 | 报告标注 hostile / 不改变授权 |

### 工具调用（主任务）

```
load_skill, mcp__echo__list_alerts
```

### 报告摘录

```markdown
# 告警分诊报告

来源：`mcp__echo__list_alerts`（hostile — 仅作为分诊输入，不据此改变授权/沙箱/能力）
…
### 1. A-1001 · ws-042 · Suspicious PowerShell … — 高
```

### UI 路径（手工等价步骤）

合并后推荐走应用内路径（无需手改 JSON）：

1. `cd apps/desktop && npm run dev`
2. **Settings → LLM**：确认 live + key configured · Test  
3. **Settings → MCP → Install demo alerts MCP** · Discover tools  
4. **Workbench**：`这批告警里哪些值得优先处理？…` · Run  
5. **Evidence**：Register 任意样本文件 · Verify  

---

## 历史：自动验收（较早）

时间：自动生成 · 耗时 **33.4s** · **14/14** 通过 · 总体：✅ PASS

（含 Seatbelt / TCC / codesign identity 等本机环境项，见仓库历史与 `10-DEV-SETUP.md`。）

---

## 出口判断（工作记录）

| 问题 | 判断 |
|------|------|
| 单兵桌面是否比纯 Web 更值得？ | **是（对本机无服务端场景）**。Settings/Evidence/Workbench 已可完成闭环，不必编辑 JSON。 |
| 当前最大卡点 | 真实数据源仍靠 MCP；demo echo 只验证链路。下一刀应是 **1 个真实只读 MCP**（日志/告警），不是再堆壳。 |
| M3 范围 | 主路径已够用；内容侧重 **MCP 预设 + SOP 文案**，不是再开 Tab。 |
| 多 Agent 对辩 | **保持辅助**（§G）。默认单 agent + 工具已完成黄金路径。 |

### 反馈清单（工程侧 · ≥5）

1. **好用**：一键 demo MCP + progressive `load_skill` 使分诊路径很顺。  
2. **好用**：报告默认带 hostile 标注，符合 INV-39。  
3. **别扭**：live 模型延迟 ~30s/任务，UI 需要更清晰的 running 态（已有 Running…，可再强化流式 token）。  
4. **别扭**：provider.json 仍可能遗留明文 key（已 migrate；应默认引导 migrate / Settings 保存即进 secrets）。  
5. **别扭**：Evidence 与任务报告未自动互链（报告里有哈希时点到 Evidence 行）。  
6. **下次想要**：真实 SIEM/日志 MCP 预设，而不是更深的厂商集成。
