# 2026年9月18日测试报告修复记录

对应《CyberGuard 深入测试报告》，修改位于 codex/release-candidate。

| 报告项 | 代码核对与处理 | 部署后验证 |
| --- | --- | --- |
| #1 Master 未绑定模型 | 配置缺失。新增对话就绪检查与明确的 AUTO 配置提示，保留供应商与模型成对绑定规则 | 管理员在 Settings → Master Agent 选择已配置供应商及其聊天模型并保存，再验证 AUTO 对话与审批闭环 |
| #2 模型切换回 AUTO | 当前代码已保留直接选择的供应商与模型。新增 Master 未绑定时选择 MiniMax 并成功发送的回归覆盖；过滤嵌入模型 | 选择具体已验证聊天模型，确认请求包含同一供应商与模型 |
| #3 安全阈值越界 | 前端范围钳制，后端限制登录次数 3–20、会话时长 5–480 分钟 | UI 输入 999；API 越界输入返回 422 |
| #4 KB 默认供应商未配置 | 新建表单模型目录只取启用且已配置凭据、支持嵌入的供应商 | 只有 MiniMax 配置时，默认选择 embo-01 |
| #5 日志窗口搜索 | 搜索在服务端分页前执行，统计使用相同条件；前端防抖且忽略过期响应 | 50 条窗口搜索历史 login，命中窗口外记录 |
| #6 删除无确认 | 当前 rc 已有 Agent、Skill、Tool、Provider、MCP、EnvVar、Template、Backup 删除确认，无需重复添加 | 点击删除后取消，数据应保留 |
| #7 会话过期静默失败 | JSON、上传、文件读取与 SSE 共用失效处理；清除 token、通知主界面退回登录，并显示过期提示 | 长时间测试后创建、上传、发送均应回到登录页 |
| #8 用量日期切换 | 当前代码已使用 YYYY-MM-DD 请求格式。补上加载/错误态日期选择器和重试，以及 7/90 天切换回归测试；尚未确认报告线上环境的失败根因 | 部署匹配版本的前后端后测试 7/30/90 天；若仍失败，检查失败请求的状态与响应 |
| #9 角色入口 | 按后端角色权限过滤导航、最近页面、搜索结果及修改/执行按钮；无权旧页面自动回退到可用页面 | Viewer 无用户管理/安全/备份入口且不可修改 Agent/Skill/Tool；Auditor 可读审计；Approver 可处理审批 |

工具 TEST 另增加页面内持久结果，显示执行状态、退出码和输出/错误。

本次未连接或修改报告中的线上部署；已停用测试账号保持原状。未测试的注入、IDOR 等专项不是本报告已确认的漏洞，也不计为本次已修复项目。

验证：前端类型检查与生产构建、ESLint、67 项前端测试通过；安全阈值、用量、Master 治理、嵌入目录、会话超时及新增报告回归共 47 项后端测试通过。
